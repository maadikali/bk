<!DOCTYPE html>

<html lang="en" xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta charset="utf-8" />
    <title>Authorize</title>
    <script src="https://code.jquery.com/jquery-3.3.1.min.js"></script>
    <link rel="stylesheet" type="text/css" href="css/registration.css" />
    <link rel="stylesheet" type="text/css" href="css/style.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</head>
<body style="background-color: #edf8ff";
onload="<%if(!b){%> alert('No user')<%} else if(!p){%>alert('Passwords dont match')<%}
else if(h == 0){%>alert('email already used!')<%}
else if(h == 1){%>alert('password must be at least 8 chars!')<%}
else if(h == 50){%>alert('fields are empty')<%}
else if(h == 2){%>alert('password must contain upper, lower and special characters!')<%}%>">
    <header class="header">
        <ul>
            <li style="float:right">
               <button class="btn" style=" font-size: 25px; color: white; margin: 6px; background-color: #f15726" onclick="location.href='index.html'"> BookStore</button>
            </li>
        </ul>
    </header>
    <div class="accountblank" style="background: white">
        <form class="login-form" ; id="form" style="margin-top: 3%;" action="/check" method="post">
            <h2 style="text-align:center; margin-top:5%">Log In</h2>
            <br />
            <div class="inputpole">
                <input type="email" id="lname" name="lname" placeholder="Enter Your Email" required/>
            </div>
            <div class="inputpole">
                <input type="password" id="lpassword" name="lpassword" placeholder="Enter Your Password" required />
                <i class="bi bi-eye-slash" id="ltogglePassword"></i>
            </div>
            <div class="inputpole">
                <button class="button" style="padding-top: 8px; border-radius: 0;" type="submit">Log In</button>
            </div>
            <hr style="width:85%; margin: auto" />
            <br />
            <a class="js" style="text-decoration: none" href="#"><h5 style="text-align: center; font-family: 'Roboto', sans-serif;">Register</h5></a>
            <div class="mssg bg-danger">
                <span id="check"></span>
            </div>
            <div id="LangTable">
                <a href="/profile" id="aa"></a>
            </div>
        </form>
        <form class="register-form" ; id="form" style="margin-top:3%;" action="/registering" method="post">
            <h2 style="text-align:center; margin-top:5%">Sign-Up</h2>
            <br />
            <div class="inputpole">
                <input type="text" id="rusername" name="rusername" placeholder="Username" value="<%=u%>" required/>
            </div>
            <div class="inputpole">
                <input type="email" id="remail" name="remail" placeholder="Enter Email" value="<%=e%>" required/>
            </div>
            <div class="inputpole">
                <input type="password" name="rpassword" id="rpassword" placeholder="Enter Password" required/>
                <i class="bi bi-eye-slash" id="rtogglePassword"></i>
            </div>
            <div class="inputpole">
                <input type="password" name="rconfirm" id="rconfirm" placeholder="Confirm Password" required/>
                <i class="bi bi-eye-slash" id="ctogglePassword"></i>
            </div>
            <div class="inputpole">
                <button class="button" style="padding-top: 8px; border-radius: 0;" type="submit">Sign-Up</button>
            </div>
            <hr style="width:85%; margin: auto" />
            <br />
            <div class="mssg bg-danger">
                <span id="check"></span>
            </div>
            <div id="LangTable">
                <a href="/profile" id="aa"></a>
            </div>
            <a style="text-decoration: none" class="js" href="#"><h5 style="text-align: center; font-family: 'Roboto', sans-serif;">Log-In</h5></a>
        </form>
        <br />
    </div>
    <script>
        $('a.js').click(function () {
            $('form').animate({ height: "toggle", opacity: "toggle" }, "slower");
        });
    </script>
    <script>
        const ltogglePassword = document.querySelector("#ltogglePassword");
        const lpassword = document.querySelector("#lpassword");
        const rtogglePassword = document.querySelector('#rtogglePassword');
        const rpassword = document.querySelector('#rpassword');
        const ctogglePassword = document.querySelector('#ctogglePassword');
        const rconfirm = document.querySelector('#rconfirm');

        ltogglePassword.addEventListener("click", function () {
            const type = lpassword.getAttribute("type") === "password" ? "text" : "password";
            lpassword.setAttribute("type", type);

            this.classList.toggle("bi-eye");
        });
        rtogglePassword.addEventListener("click", function () {
            const type = rpassword.getAttribute("type") === "password" ? "text" : "password";
            rpassword.setAttribute("type", type);

            this.classList.toggle("bi-eye");
        });
        ctogglePassword.addEventListener("click", function () {
            const type = rconfirm.getAttribute("type") === "password" ? "text" : "password";
            rconfirm.setAttribute("type", type);

            this.classList.toggle("bi-eye");
        });
    </script>
</body>
</html>
